import 'package:flutter/material.dart';


class LinkWalletScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Link Wallet')),
      body: Center(child: Text('Link Wallet Page')),
    );
  }
}
